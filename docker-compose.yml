version: '3.8'

services:
  orca_plus:
    container_name: orca_plus
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=orca_plus